{
  "manifest_version": 3,
  "name": "SM System — TradingView Bridge",
  "version": "1.0.0",
  "description": "Route symbol clicks from the /relatr dashboard into a single reused TradingView tab. Works around cross-origin isolation (COOP) that severs plain window.open named-target reuse.",

  "permissions": ["tabs"],

  "host_permissions": [
    "https://www.tradingview.com/*",
    "http://127.0.0.1:8001/*",
    "http://localhost:8001/*"
  ],

  "background": {
    "service_worker": "background.js"
  },

  "content_scripts": [
    {
      "matches": [
        "http://127.0.0.1:8001/*",
        "http://localhost:8001/*"
      ],
      "js": ["content.js"],
      "run_at": "document_end"
    }
  ]
}
